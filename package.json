{
  "scripts": {
      "build": "npx cypress run",
    "install": "npm i --save-dev cypress-mochawesome-reporter"
  },
  "devDependencies": {
    "cypress": "^13.13.1",
     "cypress-mochawesome-reporter": "^3.8.2"
  } 
}
